# Mars Rover in JavaScript

## Requirements

- Node 16.17.X

## Running

To run the application first you need to install the dependencies.

`npm install`

Then its just:

`npm run dev`

To run tests:

`npm test`
